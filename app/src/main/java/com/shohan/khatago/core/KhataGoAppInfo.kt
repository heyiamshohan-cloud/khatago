package com.shohan.khatago.core

/**
 * Identity constants for the app. Kept in code (instead of the generated
 * BuildConfig) so they are visible wherever they are used and cannot drift from
 * the release version.
 */
object KhataGoAppInfo {
    const val APP_NAME = "KhataGo"
    const val VERSION_NAME = "1.0.0"
    const val VERSION_CODE = 1
    const val DEVELOPER = "Shohan Khan"
    const val CONTACT_EMAIL = "helloiamshohan@gmail.com"
    const val TAGLINE = "All your finances, in one place."
}
